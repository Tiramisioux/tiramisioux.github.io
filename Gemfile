source "https://rubygems.org"

# GitHub Pages builds this site with Jekyll 3.x and no plugins, and the layout
# and stylesheet live in this repo rather than in a theme gem. Pinning the same
# major version locally keeps the preview honest.
gem "jekyll", "~> 3.10"
